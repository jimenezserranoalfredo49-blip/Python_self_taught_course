#Type casting is the process that converts one variable from one type to another
# They are very intuitive, using the codes: str(), int(), bool(), float()

name = "Bro code"
age = 25
gpa = 3.2
is_student = True

age = str(age)
age +="1"

print(age)

name = bool(name)
#with the bool() function you will be returned false only if there are no typed characters in the initial variable