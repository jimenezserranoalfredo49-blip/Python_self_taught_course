#use of the +, which works as well in this concrete use as the "*" or the "/"
#to express a^n, we use "**" instead of "^"
#to see the remainder we use the "%", which displays the remainder of a division, useful for even or odd numbers

friends = 0

friends += 1

print(friends)

#round() function approximates the argument which of course has to be a float to make sense, if you insert a second integer it will use it as the amount of numbers it has to round it to
#abs() function works like in mathlab
#pow() function takes a first number and makes an exponential operation when you enter the exponent as the second number
#max() function takes an "n" amount of arguments and displays the maximum value
#min() function does the opposite to the max() function
x = 3.14
y = 4
z = 5

result = round(x)

print(result)

#use "import math" to unlock the other mathematical functions
#math.ceil() rounds the number up
#math.floor() rounds the number down
#as you can see, when you import a certain module "n", to call its method (not function) you need the expression "n.function"

